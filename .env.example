# Google Drive Configuration
FOLDER_ID=your_folder_id_here

# Google Service Account Credentials (base64 encoded)
# Para producción (Railway): codifica tu credenciales.json en base64
# Ejemplo: cat credenciales.json | base64 -w 0
GOOGLE_CREDENTIALS_BASE64=your_base64_credentials_here

# OpenAI API Key (para búsqueda semántica con embeddings)
# Obtén tu API key en: https://platform.openai.com/api-keys
# Modelo usado: text-embedding-3-small (~$0.02 por 1M tokens)
OPENAI_API_KEY=sk-your-openai-api-key-here

# PostgreSQL Database URL (para persistir embeddings)
# Formato: postgresql://user:password@host:5432/database
# Plan gratuito de Render: 256 MB, suficiente para ~17 PDFs
# Ver POSTGRESQL_SETUP.md para instrucciones de configuración
DATABASE_URL=postgresql://user:password@host:5432/database_name
